FROM node:22-alpine
RUN npm install -g pnpm
WORKDIR /app
COPY . .
RUN pnpm install --no-frozen-lockfile
RUN pnpm --filter @workspace/fitness-app build
RUN npm install -g serve
CMD ["serve", "-s", "artifacts/fitness-app/dist", "-l", "3000"]
