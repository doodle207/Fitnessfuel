[build]
  command = "pnpm install && pnpm --filter @workspace/fitness-app run build"
  publish = "artifacts/fitness-app/dist/public"

[build.environment]
  BASE_PATH = "/"
  NODE_VERSION = "24"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
